name: cron

# Hits the /api/cron/* routes on a schedule. Each job calls one endpoint
# with the CRON_SECRET as a Bearer token. Jobs are independent; one failing
# doesn't block the others.
#
# Required GitHub repo secrets:
#   APP_BASE_URL    e.g. https://invoicechase.com
#   CRON_SECRET     same value set on the deployed app

on:
  schedule:
    # Daily at 12:00 UTC ≈ 7am ET — owner-facing AR digest email.
    - cron: "0 12 * * *"
    # Daily at 14:00 UTC ≈ 9am ET — covers payment plan reminders and the
    # deposit poller.
    - cron: "0 14 * * *"
    # Mondays at 15:00 UTC for the weekly digest (SMS).
    - cron: "0 15 * * 1"
    # 3rd of each month at 12:00 UTC for partner commission rollup
    # (covers the prior full calendar month).
    - cron: "0 12 3 * *"
    # Every hour at :05 — fires the weekly-report endpoint. The endpoint
    # itself checks each org's local timezone + scheduled hour and only
    # sends to those whose window matches right now.
    - cron: "5 * * * *"
    # Every 15 min — drains the onboarding first-win batch send queue,
    # throttled per-org (max 5 sends/run) and only during business hours.
    - cron: "*/15 * * * *"
  workflow_dispatch: {}

jobs:
  daily:
    if: github.event.schedule == '0 14 * * *' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        endpoint:
          - deposit-poller
          - payment-plan-reminders
          - reminder-sequences
          - pre-bill-reminders
    steps:
      - name: Hit ${{ matrix.endpoint }}
        run: |
          curl -fsS \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$APP_BASE_URL/api/cron/${{ matrix.endpoint }}"
        env:
          APP_BASE_URL: ${{ secrets.APP_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}

  daily-digest:
    if: github.event.schedule == '0 12 * * *' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Daily owner digest
        run: |
          curl -fsS \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$APP_BASE_URL/api/cron/daily-digest"
        env:
          APP_BASE_URL: ${{ secrets.APP_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}

  weekly-digest:
    if: github.event.schedule == '0 15 * * 1'
    runs-on: ubuntu-latest
    steps:
      - name: Weekly digest
        run: |
          curl -fsS \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$APP_BASE_URL/api/cron/weekly-digest"
        env:
          APP_BASE_URL: ${{ secrets.APP_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}

  weekly-report:
    if: github.event.schedule == '5 * * * *' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Weekly retention report (per-org timezone-aware)
        run: |
          curl -fsS \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$APP_BASE_URL/api/cron/weekly-report"
        env:
          APP_BASE_URL: ${{ secrets.APP_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}

  first-batch-send:
    if: github.event.schedule == '*/15 * * * *' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: First-win onboarding batch drain
        run: |
          curl -fsS \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$APP_BASE_URL/api/cron/first-batch-send"
        env:
          APP_BASE_URL: ${{ secrets.APP_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}

  monthly-commissions:
    if: github.event.schedule == '0 12 3 * *'
    runs-on: ubuntu-latest
    steps:
      - name: Partner commission rollup
        run: |
          curl -fsS \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$APP_BASE_URL/api/cron/partner-commissions"
        env:
          APP_BASE_URL: ${{ secrets.APP_BASE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
