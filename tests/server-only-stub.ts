// Stub for the `server-only` package in vitest. The real package is a
// build-time guard that throws if imported in a client bundle; tests
// run in Node, so a no-op is the right behavior.
export {};
